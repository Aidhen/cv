services:
  node-cli:
    image: node:20-alpine
    working_dir: /app
    volumes:
      - .:/app

  dev-server:
    image: node:20-alpine
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "5173:5173"
    command: npm run dev -- --host 0.0.0.0